import PrePromptEditor from "@/components/common/PrePomptEditor";

const ConversationChatBuilder = () => {
  return (
    <main className="flex flex-col items-center w-full max-w-2xl py-8 mx-auto space-y-4">
      <PrePromptEditor />
    </main>
  );
};

export default ConversationChatBuilder;
